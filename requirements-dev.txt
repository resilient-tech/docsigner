# Development dependencies: everything needed to run the Python test suites.
# Runtime deps come from each component's own package (core/, server/).
#
#   pip install -e ./core -e ./server
#   pip install -r requirements-dev.txt
#   pytest core/tests server/tests
#   PYTHONPATH=desktop/backend pytest desktop/backend/tests
#
# The token host is not Python: it is host-rs, tested with `cargo test`.

pytest
httpx        # FastAPI's TestClient needs it for server/tests
cryptography # core/tests and desktop/backend/tests build keys and certificates
