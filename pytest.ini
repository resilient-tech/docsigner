[pytest]
# core/tests is the one home for the fixtures every suite needs (a blank PDF, a
# self-signed cert, a token-style hash signature). Before this, server/tests and
# e2e/ each carried their own byte-identical copy.
pythonpath = core/tests
