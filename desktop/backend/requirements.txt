fastapi>=0.115
uvicorn[standard]>=0.30
pypdfium2>=4
# The signing engine and token host, from the repo (run pip from this backend/
# directory so the relative paths resolve). Pulls pyHanko, cryptography, qrcode.
-e ../../core
-e ../../host
